# Frontend

Static files that power the password strength checker demo. No build tools or package managers are required.

## Usage

1. Open `frontend/index.html` in a browser.
2. Type a password into the input field to watch the meter, score, and suggestions update in real time.

The JavaScript file (`app.js`) evaluates the password as you type, adjusting the meter and keeping the suggestions list in sync with each change.
